// run in browser console on the verify-callback page
const params = new URLSearchParams((location.hash || '').replace('#','?'));
// Intentionally left blank: avoid logging sensitive tokens in production.